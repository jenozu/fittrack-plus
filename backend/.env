# Database Configuration
# Replace 'postgres' with your PostgreSQL username if different
# Replace 'fittrack123' with the password you just set
# Replace 'localhost' with your database host if different
# Replace '5432' with your PostgreSQL port if different
# Replace 'fittrack_db' with your database name if different
DATABASE_URL=postgresql://postgres:K4lisbae!!@localhost:5432/fittrack_db

# Security Configuration
# IMPORTANT: Generate a secure secret key for production
# You can generate one by running: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production-please

# External API Keys (Optional - System uses mock data if not provided)
# These are completely optional for development
# The app will work perfectly fine without them using mock data

# Nutritionix API (Optional)
# Sign up at: https://developer.nutritionix.com/
# Leave blank to use mock data
NUTRITIONIX_APP_ID=
NUTRITIONIX_APP_KEY=

# USDA FoodData Central API (Optional)
# Sign up at: https://fdc.nal.usda.gov/api-key-signup.html
# Leave blank to use mock data
USDA_API_KEY=